/* sourcemaking.com
 *  
 * The intent of a Null Object is to encapsulate the absence of an object by providing a 
 * substitutable alternative that offers suitable default do nothing behavior. 
 * In short, a design where "nothing will come of nothing" 
 * 
 * Given that an object reference may be optionally null, and that the result of a null check 
The Null Object class is often implemented as a Singleton. Since a null object usually does not have any state, its state can't change, so multiple instances are identical.1
 * MAINLY for JAVA
 * */
 
 
